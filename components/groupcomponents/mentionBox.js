import React, { useState } from 'react'

export const MentionBox = ({setMention}) => {
    const [open, onOpen] = useState(false);
    const [select, setSelect] = useState([]);
  return (
    <></>
  )
}
